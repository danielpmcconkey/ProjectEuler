module DomainTypes

type PrimeStarterPack = {
    primes:int[];
    primeBools:bool[];
}
type Fraction = {
    numerator:int;
    denominator:int;
}
type IntegerTriangle = {
    aLength:int;
    bLength:int;
    cLength:int;
    perimeter:int;
}